def get_infected_percentage(num):
  num = int(num)
  if num >= 0:
     return ( 1 * int(num))
  else:
    print("invalid entry")

try :
  infected_percentage = input("please enter the infected percentage numbers : ")
  print(get_infected_percentage(infected_percentage))



except:
 print("error")

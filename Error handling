def verify_int(max_percent):
    while True:
        try:
            input_percent = int(input())
            if input_percent > 0 and input_percent < max_percent:
                return input_percent
            else:
                print("The number of infected must be between 0 and", max_percent)
        except:
            print("Error! You can only put in Integers")
